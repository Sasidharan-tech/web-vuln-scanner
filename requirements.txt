# Web Vulnerability Scanner - Dependencies (merged: native scanner + OSTE Meta-Scan)

# HTTP requests
requests>=2.31.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# URL handling
urllib3>=2.0.0

# Database
# (using built-in sqlite3)

# Report generation
jinja2>=3.1.0

# CLI colors (optional but recommended)
colorama>=0.4.6

# ---- FastAPI unified backend (replaces Flask) ----
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
websockets>=12.0
pydantic>=2.5.3
aiofiles>=23.2.1

# ---- OSTE Meta-Scan optional tool integrations ----
# Install zaproxy only if you want OWASP ZAP support:
#   pip install zaproxy>=0.5.0
python-dotenv>=1.0.0
